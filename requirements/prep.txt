# USAM Phase A (T1 — A100 Slurm prep) deps.
# Installed on top of requirements/base.txt by docker/Dockerfile.prep_a100.
#
# Phase A is download → flow/depth compute → DINO caching → HF upload.
# These deps are NOT needed at training (T2) or eval (T3) time.

# --- optical flow (SEA-RAFT) ---
# Not on PyPI. Installed via git clone in docker/Dockerfile.prep_a100:
#   clone of github.com/princeton-vl/SEA-RAFT to /opt/searaft
#   A sea_raft/ shim package is created under /opt/searaft so that
#   `from sea_raft.api import load_searaft` resolves correctly.
# sea-raft

# --- depth (Depth-Anything-V2) ---
# Not on PyPI. Installed via git clone in docker/Dockerfile.prep_a100:
#   clone of github.com/DepthAnything/Depth-Anything-V2 to /opt/dav2
#   Missing __init__.py files are created so the package is importable.
# depth-anything-v2

# --- video decoding for prep workers ---
decord
opencv-python

# --- HF Hub upload daemon (CommitScheduler + upload_large_folder) ---
# `hf_xet` and the CLI extras enable the high-perf transfer path used by
# prep/_hub.py.
huggingface-hub[hf_xet,cli]
hf_transfer
safetensors

# --- RLDS readers (DROID, Bridge, OXE-AugE) ---
tensorflow-datasets
# Google Cloud Storage filesystem driver (TFDS uses it to read gs:// URIs
# like the public DROID bucket at gs://gresearch/robotics).
gcsfs
